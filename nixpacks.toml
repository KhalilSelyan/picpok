[phases.setup]
nixPkgs = ['nodejs_22', 'nodePackages.pnpm']
cmds = ['corepack enable']

[phases.install]
cmds = ['pnpm install --frozen-lockfile']

[phases.build]
cmds = ['pnpm run build']

[start]
cmd = 'pnpm run start'
